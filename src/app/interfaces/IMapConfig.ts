export interface IMapConfig {
    width: number,
    height: number,
    positions: number,
    padding: number,
    minimalPositionDistance: number,
    pointSize: number,
    generationStrengthPercent: number,
}